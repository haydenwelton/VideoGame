if (sprite_index == Run) audio_play_sound(choose(Step1,Step2,Step3),1,false);
if (sprite_index == RunArms) audio_play_sound(choose(Step1,Step2,Step3),1,false);
if (sprite_index == Sprint) audio_play_sound(choose(Step1,Step2,Step3),1,false);
if (sprite_index == SprintArms) audio_play_sound(choose(Step1,Step2,Step3),1,false);
if sprite_index = Melee
{
	if (attacking = true)
	{
		attacking = false;
	}
}
